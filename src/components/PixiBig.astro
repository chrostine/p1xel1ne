---
const { billedet } = Astro.props;
const { hojden } = Astro.props;
const { bredden } = Astro.props;
---

<div class="fixed inset-x-0 z-1 flex pointer-events-none select-none hidden xl:flex mt-[300px] xl:ml-10 2xl:ml-20" style={`top: ${hojden}px`}>
    <img src={billedet} alt="" width=`${bredden}px` class="transform scale-x-[-1]">
</div>