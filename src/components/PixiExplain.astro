---
const { billedet } = Astro.props;
const { hojden } = Astro.props;
const { bredden } = Astro.props;
---

<div class="fixed inset-x-0 z-1 flex justify-center pointer-events-none select-none hidden xl:flex" style={`top: ${hojden}px`}>
    <img src={billedet} alt="" width=`${bredden}px`>
</div>