---
const { billedet } = Astro.props;
const { hojden } = Astro.props;
const { bredden } = Astro.props;
---

<div class="fixed inset-x-0 z-3 flex justify-end pointer-events-none select-none hidden xl:flex xl:mr-10 2xl:mr-20 rotate-8" style={`top: ${hojden}px`}>
    <img src={billedet} alt="" width=`${bredden}px`>
</div>